# ANTIGRAVITY.md - Antigravity Bridge

Read `AGENTS.md` first, then read `DESIGN.md` before any design, UI, SVG, Phaser scene, port, trade, character, boat, creature, or mobile layout work.

This project uses Google Labs `DESIGN.md` as the visual source of truth. Validate changes with `npm run design:lint`.

For OpenClaw-related context, follow the bridge paths listed in `AGENTS.md` and do not copy secrets or raw private logs into project files.
