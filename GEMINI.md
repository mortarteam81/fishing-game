# GEMINI.md - Gemini CLI Bridge

Read `AGENTS.md` first, then read `DESIGN.md` before any design, UI, SVG, Phaser scene, port, trade, character, boat, creature, or mobile layout work.

Use the local Google Labs `design.md` CLI through `npm run design:lint` to validate `DESIGN.md`. Use `npm run design:spec` when you need the format reference.

Keep game visuals original, family-friendly, premium, and consistent with the 반짝바다 commercial art direction.
