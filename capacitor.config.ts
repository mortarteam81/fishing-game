import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.mortarteam81.banjjakbada",
  appName: "반짝바다 낚시단",
  webDir: "dist",
  server: {
    androidScheme: "https",
  },
};

export default config;
