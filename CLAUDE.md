# CLAUDE.md - Claude Code Bridge

Read `AGENTS.md` first, then read `DESIGN.md` before any design, UI, SVG, Phaser scene, port, trade, character, boat, creature, or mobile layout work.

This repository has `@google/design.md` installed locally. Use `npm run design:lint` after editing `DESIGN.md`, and keep the Google `design.md` token front matter in sync with the visual direction described in the markdown body.

Do not copy OpenClaw tokens, OAuth data, private memory, or raw logs into prompts or files.
